[paths]
raw_data_dir = /path/to/input_data
graph_cache_dir = /path/to/
run_dir = /path/to/train_model

[dataset]
name = dataset_name
orbital_spec_file = config/orbital.json

[task]
target = T

[runtime]
use_gpu = True
device = cuda:0
data_workers = 0

[optimizer]
lr = number
epochs = number
batch_size = number

[split]
train = number
validation = number
test = number

[model]
circuit_layers = number
qbits = number
